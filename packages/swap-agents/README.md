# swap-agents

Schedule and manage background agents for [swap](https://github.com/SwapnilPande/swap) — named cron jobs that run a command or script on a schedule.

Install with:

```bash
swap plugins install agents
```

See [docs/agents.md](../../docs/agents.md) for full usage.
